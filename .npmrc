
# Force npm as package manager
node-linker=hoisted
enable-pre-post-scripts=true

# Prevent yarn from being used
engine-strict=true

# Use npm for all operations
prefer-offline=false
audit=false
fund=false

# Explicitly disable Corepack to prevent yarn interference
enable-corepack=false

# Force npm usage
package-manager=npm
